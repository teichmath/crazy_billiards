package edu.rice.comp504.model.cmd;

import edu.rice.comp504.model.paint.Ball;

/**
 * A command that needs to be excuted by a ball
 */
public interface IBallCmd {

    /**
     * Execute the command by the receiver (context).
     * @param context  The receiver that will execute the command.
     */
    public void execute(Ball context);
}
